<header class="header">
    <div class="container">
        <div class="header__wrapper">
            <div class="header__logo">
                <div class="logo">Alina Visions</div>
{#                {{ site_logo() }}#}
            </div>

            <div class="header__right">
                <div class="header__menu">
                    {{ menu('header') }}
                </div>

                <div class="header_actions">
                    {% if page is defined %}
                        {{ language_switcher(page) }}
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</header>